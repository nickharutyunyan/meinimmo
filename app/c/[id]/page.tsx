import { notFound } from 'next/navigation'; import { comparison, report } from '@/lib/store'; import { reportTitle } from '@/lib/display';
const money=(n:number)=>new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(n);
export default async function Compare({params}:{params:Promise<{id:string}>}) { const item=await comparison((await params).id);if(!item)notFound();const [a,b]=await Promise.all(item.reportIds.map(report));if(!a||!b)notFound();const row=(label:string,x:string,y:string)=><><div className="metric">{label}</div><div>{x}</div><div>{y}</div></>;return <main className="comparison-page"><a className="logo" href="/">h <span>habitat</span></a><p className="eyebrow">PROPERTY COMPARISON</p><h1>Two homes, side by side.</h1><p className="lead">A calm view of the facts that change a decision.</p><section className="comparison-grid"><div className="metric">PROPERTY</div><div><small>OPTION A</small><h2>{reportTitle(a)}</h2></div><div><small>OPTION B</small><h2>{reportTitle(b)}</h2></div>{row('Asking price',money(a.facts.price),money(b.facts.price))}{row('Price per m²',a.facts.area?money(a.facts.price/a.facts.area):'—',b.facts.area?money(b.facts.price/b.facts.area):'—')}{row('Living space',`${a.facts.area} m²`,`${b.facts.area} m²`)}{row('Rooms',a.facts.rooms,b.facts.rooms)}{row('Floor',a.facts.floor,b.facts.floor)}{row('Energy',a.facts.energy,b.facts.energy)}{row('Habitat score',`${a.score.toFixed(1)} / 10`,`${b.score.toFixed(1)} / 10`)}</section></main> }
